server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/winter_olympics_test_db
    username: hristiyanivanov
    password: BrockMakov34
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml